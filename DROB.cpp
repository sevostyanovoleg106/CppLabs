// DROB.cpp: определяет точку входа для консольного приложения.
//

#include "stdafx.h"
#include <iostream>
#include  <cmath> 

using namespace std;

 double main()
{
	double x, y, z, a, b;
	cin >> x;
	cin >> y;
	cin >> z;
	a = ((2 * sin(y + PI / 3)) / (1 / 2 + pow(sin, 3)(pow(sin, 3)* abs(y + 2))) + pow(sin, -1)(x + PI / 2);
	cout << "занечние а" << a << endl;
	b = 1 + ((pow(z, 2) / 5) / (3 - pow(z, 2) / 3);
	cout << "значение b" << b << endl;
	

     return 0;
}

